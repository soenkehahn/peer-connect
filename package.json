{
  "name": "@shahn/peer-connect",
  "version": "0.0.0",
  "packageManager": "yarn@3.2.1",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepack": "jest && rm -rf dist && tsc"
  },
  "devDependencies": {
    "@types/jest": "^28.1.2",
    "esbuild": "^0.14.45",
    "esbuild-jest": "^0.5.0",
    "jest": "^28.1.1",
    "typescript": "^4.7.4"
  }
}
